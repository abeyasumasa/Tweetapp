
     <h1>Tweet一覧</h1>
     <p><%= notice %></p>

       <% @tweets.each do |tweet| %>
         <div class="tweet">
            <%=  tweet.content %><br>
            <%= link_to'投稿内容確認',tweet_path(tweet.id)%>
            <%= link_to'投稿内容編集',edit_tweet_path(tweet.id),data: { confirm: '本当に編集していいですか？'}%>
            <%= link_to'投稿削除',tweet_path(tweet.id),method: :delete,data: { confirm: '本当に削除していいですか？' }%><br>
        </div>
       <% end %>
    <%=link_to '新しくツイートする',new_tweet_path %>
